#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints the last digit of the random
 * number stored in the variable n
 * Return: Always 0 (Success)
 */
int main(void)
{
char a = 'a';
int n = '0';
while (0 < 26)
{
putchar (a);
n++;
a++;
}
putchar('\n');
return (0);
}
